server:
  port: 8000
  servlet:
    context-path: /user-service

spring:
  jpa:
    generate-ddl: true
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/kayo
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver